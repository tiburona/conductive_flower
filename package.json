{
  "name": "conductive_flower",
  "version": "1.0.0",
  "scripts": {
    "start": "open http://127.0.0.1:8080/index.html && http-server -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "http-server": "^0.11.1"
  }
}
